server:
  port: 1112
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://dev1:1111/eureka/,http://dev3:1113/eureka/
  instance:
    hostname: dev2
    prefer-ip-address: false
spring:
  application:
    name: eureka-server